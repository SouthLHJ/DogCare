import { StyleSheet, Text, View } from "react-native";

function MemoriesDetailScreen() {
    return (  
    <View>
        <Text>memoriesDetail 페이지</Text>
    </View>
    );
}

export default MemoriesDetailScreen;

const styles = StyleSheet.create({
    
});