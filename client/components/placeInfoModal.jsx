import { Modal } from "react-native";

function PlaceInfoModal({visible}) {
    return (
        <Modal animationType="slide" visible={visible}>
            
        </Modal>
    );
}

export default PlaceInfoModal;